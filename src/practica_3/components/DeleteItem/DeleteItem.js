const DeleteItem = ({index}) => {
    const buttonClickHandler = (index) => {

    }

  return (
    <div>
        <button onClick={buttonClickHandler}>Eliminar</button>
    </div>
  )
}

export default DeleteItem